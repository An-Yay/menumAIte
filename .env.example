# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# Google Cloud API key.
# Required APIs enabled on the key's project:
#   - Places API (New)          -> restaurant discovery + reviews
#   - Generative Language API   -> LLM (menu extraction, translation, suggestions)
GOOGLE_API_KEY=your_key_here
