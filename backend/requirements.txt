# Web framework + ASGI server for the backend API.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0

# Settings/validation: typed domain models and env-based config loading.
# (pydantic v2 is required transitively by strands-agents as well.)
pydantic>=2.4,<3.0
pydantic-settings>=2.6,<3.0

# HTTP client for calling Google Places and for fetching restaurant websites.
httpx>=0.27,<1.0

# Fast, lenient HTML parser used by the menu crawler to extract text/links.
selectolax>=0.3.21

# Strands Agents SDK: the agent framework that orchestrates the tools.
strands-agents>=1.55,<2.0
